# CLAUDE.md

本项目使用 uv 管理 Python 环境和依赖。

- 添加依赖：`uv add <package>`
- 运行项目：`uv run main.py`
- 环境变量从 `.env` 读取（需配置 `PIXVERSE_API_KEY`）

## README.md 编辑规则

README.md 是 **append-only** 的进度日志，严禁修改已有内容。每次更新只能在文件末尾追加新的进度条目，不得修改或删除历史记录中的任何内容（包括旧的 TODO 项）。
